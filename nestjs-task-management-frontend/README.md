wouldn't do `npm i` so upgraded node-sass to version 9.0.0.
now when doing `npm start`, it throws the digital envelope routines error.
Now it errors with 
  `Error: Node Sass version 9.0.0 is incompatible with ^4.0.0.`










# Task Management Application Front-end

This application acts as the front-end for the Task Management application developed throughout the _NestJS - Zero to Hero_ course, produced by Ariel Weinberger.
